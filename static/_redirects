# /p/demo/* is proxied by functions/p/demo/[[path]].ts to the
# 1context-demo Pages project — see that file for the rationale.
# Pages Functions take precedence over static files, so this is just
# a safety net for the bare /p/demo path that some browsers may not
# auto-trail-slash before hitting the function.
/p/demo  /p/demo/  301
